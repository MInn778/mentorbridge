# 진행 기록 & TODO

## ✅ 지금까지 완료한 것

### 인증/보안
- 리프레시 토큰 (JWT access+refresh, 재발급 시 로테이션) + **모든 페이지에 자동 재시도 연결 완료**
  (401 오면 `AuthContext`가 window.fetch를 감싸서 자동 재발급 후 재시도)
- 구글 OAuth2 로그인 (쿠키 기반 인증요청 저장소, 순환참조/한글 인코딩/프론트 무한루프 버그 수정)
- 계정 정지(`is_suspended`) 기능을 실제 로그인 차단에 연결
- oauth2Login 설정 후 미인증 `/api/**` 요청이 구글로 리다이렉트되던 버그 수정 (401로 정상화)
- **버그 수정**: 만료/손상된 토큰이 500 에러를 내던 것 → 깔끔한 401로 처리 (안 고쳤으면 리프레시 자동재시도 자체가 무용지물이었음)
- `/api/admin/**`을 ADMIN 역할만 접근하도록 제한

### 채용정보 (Recruitment)
- Bookmark + JobCache 백엔드/프론트 구현, 검색 기능
- ⚠️ `job_cache`는 실제 채용 API 없이 심어둔 샘플 5건 — **실제 API 연동은 다른 팀원 작업 예정**

### 대시보드 (나의 활동)
- 하드코딩 통계 → 실제 API 연동, 4개 카드 클릭 시 실제 목록 펼쳐짐
- 멘토링 탭에서 대기중인 신청에 승인/거절 버튼
- 공지사항 팝업 (하루/일주일 보지 않기, localStorage로 기억)

### 커뮤니티/스터디
- 검색 기능, 카테고리 "기타" 탭
- 모집 완료 토글 + 상태 배지, 완료 시 지원 버튼 비활성화
- 댓글 수/참여 인원 실제 값 표시, 인원 수 제한 + 정원 초과 시 수락 차단
- 글쓴이 작성 즉시 참여자 자동 등록
- **지원 사유 입력 모달** — 지원 클릭 시 고정 문구 대신 실제 사유를 입력하고 전송
- **게시글 신고 기능** — 신고 접수 → 관리자 페이지에서 접수/처리중/완료 상태 관리 + 처리 결과 기록
- **버그 수정**: 지원 수락 시 스터디 그룹이 안 만들어져서 항상 500 에러였던 것 수정

### 멘토 시스템
- 멘토 신청 자동승인 제거 → 관리자 심사 체제
- 멘토 본인 프로필 셀프 수정 ("프로필 소개하기")
- 멘토링 매칭 승인/거절 버튼 (대시보드)
- **멘토링 매칭 완료 처리** — 수락(ACCEPTED) 상태인 매칭을 멘토/멘티 둘 다 "완료 처리" 가능, 완료 시 상대방에게 알림 발송
- **버그 수정**: `/api/mentors` 인증 헤더 누락으로 멘토 목록이 항상 빈 화면이었던 것 수정

### 알림
- 댓글/쪽지/멘토링 신청 시 실제 알림 발송 연결
- 알림 아이콘 대소문자 버그 수정
- **채용공고 마감 임박 알림** — 스크랩한 공고 마감이 3일 남으면 매일 오전 9시 `SCRAP` 타입 알림 자동 발송
  (관리자 페이지에서 수동 트리거 가능: `POST /api/admin/jobs/check-deadlines`)
  **버그 수정**: 처음엔 `@Transactional(readOnly = true)`로 짜서 알림 INSERT가 막혀 500 에러 — writable 트랜잭션으로 수정

### 관리자 페이지 (`/admin`)
- 사용자 관리 (정지/해제), 멘토 신청 심사, 게시글 삭제, 공지사항 작성/삭제, **신고 관리**
- **관리자 계정 지정** — 사용자 관리 탭에서 다른 사용자를 관리자로 승격 가능 (가입 화면으로 노출하는 건 보안상 위험해서 이 방식 채택)
- **버그 수정 + 재발방지**: 관리자가 스스로 멘토 신청 승인하면 ADMIN 권한이 사라지던 버그 → 복구 + 재발 방지 가드
- 관리자 계정: `admin@naver.com` / `0000`

### UI/UX
- 역할 배지(멘토/멘티/관리자) 표시 (Navbar, 프로필 페이지)
- 알림/프로필 드롭다운 바깥 클릭 시 닫힘
- 로그아웃 시 랜딩 페이지로 이동
- PostDetail 레이아웃 겹침 버그 수정

---

## 🚫 범위 제외 (다른 팀원이 별도 제작)
- 사람인/워크넷 실제 채용 공고 API 연동 (`job_cache` 스키마는 이미 준비돼 있음)
- 진로 추천 AI (`ai_recommendation` 저장 포함, `GEMINI_API_KEY` 연동)

---

## 🔜 앞으로 해야 할 것

### 애매해서 확인 필요 (AI 관련이라 다른 팀 범위일 수도 있음)
- [ ] 멘토-멘티 매칭 추천 AI — 지금은 매칭이 신청/승인만 있고 추천 로직 없음
- [ ] 피드백 게시글 AI 자동 피드백 — `aiFeedback` 필드는 전달만 되고 실제 AI 호출 없음 (`GEMINI_API_KEY` 필요)

### 운영/기타
- [ ] 로컬 MySQL root 비밀번호가 `changeme`로 변경됨 — 다른 프로젝트에서 이 MySQL 인스턴스를 쓰고 있다면 참고
